<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:fontFamily="sans-serif-light"
        android:singleLine="true"
        android:textSize="18sp"
        app:layout_constraintLeft_toRightOf="@id/startGuideline"
        app:layout_constraintRight_toLeftOf="@+id/actionsImageButton"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Harry Potter and the Methods of Rationality" />

    <TextView
        android:id="@+id/chaptersTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:textSize="16sp"
        app:layout_constraintLeft_toRightOf="@id/startGuideline"
        app:layout_constraintRight_toLeftOf="@+id/actionsImageButton"
        app:layout_constraintTop_toBottomOf="@id/titleTextView"
        tools:text="120/122 synced chapters" />

    <ImageView
        android:id="@+id/actionsImageButton"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@drawable/ic_more"
        android:tint="@color/bottomNavigationGrey"
        app:layout_constraintBaseline_toBaselineOf="@+id/titleTextView"
        app:layout_constraintRight_toLeftOf="@id/endGuideline" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/startGuideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_begin="16dp" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/endGuideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_end="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
